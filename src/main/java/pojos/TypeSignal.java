package pojos;

/**
 * Types of physiological signals supported by the system.
 * Each type is processed differently and assigned to a matching doctor specialty.
 */
public enum TypeSignal {
    ECG, EMG
}
